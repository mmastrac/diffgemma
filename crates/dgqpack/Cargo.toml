[package]
name = "dgqpack"
version = "0.1.0"
edition = "2024"
description = "The .dgq pack format: manifest types and the validating loader every backend opens a pack through"
license = "MIT"

[dependencies]
memmap2 = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
